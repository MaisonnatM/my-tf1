import styled from "@emotion/styled"

export const StyledLayout = styled.div(() => {
  return {
    backgroundColor: "#000",
    height: "100vh",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  }
})
